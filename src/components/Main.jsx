import React from 'react';
import LaunchCountDown from './LaunchCountDown';

function Main(props) {
    return (
        <section>
            <LaunchCountDown/>
        </section>
    );
}

export default Main;